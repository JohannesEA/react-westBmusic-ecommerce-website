import styled from "styled-components";

export const Headline = styled.form`
  font-size: clamp(1.5rem, 10vw, 3rem);
`;

export const UnderText = styled.form`
  font-size: clamp(1.5rem, 10vw, 3rem);
`;

export const Text = styled.form`
  font-size: clamp(0.8rem, 10vw, 1.5rem);
`;

export const SmallText = styled.form`
  font-size: clamp(0.6rem, 10vw, 1rem);
`;
